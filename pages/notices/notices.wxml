<view>通知</view>
<message-detail wx:for="{{lists}}" data="{{item}}" url="notices" bindtap="getDetail" data-id="{{item.id}}"> 
  <view slot="modal-content">
    <view>{{detail.content}}</view>
    <view>{{detail.created_at}}</view>
  </view>
</message-detail>

